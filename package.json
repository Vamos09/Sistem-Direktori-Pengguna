{
  "name": "sistem-direktori-pengguna",
  "version": "1.0.0",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.6.1",
    "jsdom": "^29.1.1",
    "vitest": "^1.6.1"
  }
}
